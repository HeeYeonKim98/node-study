const express = require("express");
const router = express.Router();

/**
 * @method POST
 * @summary view
 */
router.post("/", (req, res) => {});

/**
 * @method POST
 * @summary insert
 */
router.post("/", (req, res) => {});

/**
 * @method POST
 * @summary update
 */
router.post("/", (req, res) => {});

/**
 * @method POST
 * @summary delete
 */
router.delete("/", (req, res) => {});

module.exports = router;
